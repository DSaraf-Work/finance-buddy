# ============================================================================
# Finance Buddy Environment Variables
# ============================================================================
#
# IMPORTANT: Single-Environment Configuration
# This configuration uses the SAME credentials for both local development
# and production deployment on Vercel.
#
# For local development (.env.local):
#   - Use these values as-is
#   - Set NEXTAUTH_URL=http://localhost:3000
#
# For production (Vercel environment variables):
#   - Copy ALL values from your .env.local
#   - ONLY change NEXTAUTH_URL to your Vercel deployment URL
#   - Everything else stays the same
# ============================================================================

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
# ⚠️ SAME for both local and production
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Gmail OAuth Configuration
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# ⚠️ SAME for both local and production
# Authorized redirect URIs should include BOTH:
#   - http://localhost:3000/api/gmail/callback (for local)
#   - https://your-domain.vercel.app/api/gmail/callback (for production)
GMAIL_CLIENT_ID=your-gmail-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-gmail-client-secret

# Application Configuration
# ⚠️ THIS IS THE ONLY VARIABLE THAT CHANGES BETWEEN ENVIRONMENTS
# Local development: http://localhost:3000
# Production (Vercel): https://your-domain.vercel.app
NEXTAUTH_URL=http://localhost:3000
COOKIE_NAME=fb_session

# AI Service API Keys (Optional - for transaction extraction features)
# ⚠️ SAME for both local and production (shared API quotas)
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google AI: https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY=your-google-ai-key-here

# ============================================================================
# Deployment Notes
# ============================================================================
#
# When deploying to Vercel:
# 1. Copy ALL values from your .env.local to Vercel environment variables
# 2. Change ONLY the NEXTAUTH_URL to your Vercel deployment URL
# 3. Everything else (Supabase, Gmail OAuth, AI keys) stays the same
#
# This means:
# ✅ Same database for dev and production (shared data)
# ✅ Same Gmail OAuth credentials (shared connections)
# ✅ Same AI API keys (shared quotas)
# ⚠️ Development and production data will coexist in the same database
#
# Future Recommendation:
# For a production application, create separate:
# - Supabase project for production
# - Google OAuth credentials for production
# - AI API keys for production
#
# See VERCEL_DEPLOYMENT.md for migration guide.
# ============================================================================
